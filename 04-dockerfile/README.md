# Dockerfile

This folder contains my learning and notes about Dockerfiles and building Docker images.

## Purpose

The purpose of this folder is to understand:

* What a Dockerfile is
* Dockerfile instructions
* Base images
* Working directories
* Copying files into images
* Installing dependencies
* Running commands during image builds
* Starting containers with default commands
* Building custom Docker images

